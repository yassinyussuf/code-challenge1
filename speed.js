// prompt the user to enter his details
let prompt = require("prompt-sync")({ sigint:true });

let name = prompt("can you put your name: ");
let speed = prompt("can you put your speed: ");

//the line below is the code that will be executed if the user enters
Improve 
function speeding(speed){
    if (speed <70){
        return "ok";
    } else {
        let demeritpoints = math.trunc((speed - 70) / 5);
    }
}

